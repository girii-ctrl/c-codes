#include<stdio.h>
int main(){
    // the program to calculate area of a square 
    int length ;
    printf("enter the length of the square:");
    scanf("%d",&length);
    int area = length *length ;
    printf("the area of the square is :%d\n",area);
    return 0;

}